apiVersion: v1
kind: Service
metadata:
  name: vault-service
spec:
  type: NodePort
  selector:
    app: vault
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081

