spring:
  application:
    name: vault
  datasource:
    url: jdbc:h2:mem:vaultdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

